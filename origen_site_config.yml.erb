# If .erb is present, the site config will be run as an ERB first before being parsed as a YML file.

# Below is just an example. Since the statement evaluates to false, the test variable will not be present in the
# site config.
% if false
origen_erb_test: true
% end
