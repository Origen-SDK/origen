This template is used to test that the custom inspect methods of the various RGen datatypes work

Should display a list of registers:

<%= dut.nvm.regs.inspect %>

Should display an actual register:

<%= dut.nvm.regs(:mclkdiv).inspect %>
<%= dut.nvm.regs(:mclkdiv).inspect(fancy_output: false) %>

This one had problems in real life:

% dut.nvm.add_proth_reg
<%= dut.nvm.regs(:proth).inspect %>

Check the multi bitgroups scattered through the register

% dut.nvm.add_multi_split_reg
<%= dut.nvm.regs(:multi_group).inspect %>

Test of an MSB0 bit-order register

% dut.nvm.add_msb0_regs
<%= dut.nvm.regs(:SIUL2_MIDR1).inspect %>

Test of an MSB0 bit-order register displayed with msb0 numbering

<%= dut.nvm.regs(:SIUL2_MIDR1).with_msb0.inspect %>

Test of regs with non byte aligned number of bits

% dut.nvm.add_non_byte_aligned_regs
<%= dut.nvm.regs(:non_aligned_small).inspect %>

<%= dut.nvm.regs(:non_aligned_big).inspect %>
<%= dut.nvm.regs(:non_aligned_big).inspect(fancy_output: false) %>

<%= dut.nvm.regs(:non_aligned_small_msb0).inspect %>

<%= dut.nvm.regs(:non_aligned_big_msb0).inspect %>
<%= dut.nvm.regs(:non_aligned_big_msb0).inspect(fancy_output: false) %>

Test of non byte aligned msb0 regs displayed with msb0 numbering

<%= dut.nvm.regs(:non_aligned_small_msb0).with_msb0.inspect %>

<%= dut.nvm.regs(:non_aligned_big_msb0).with_msb0.inspect %>
<%= dut.nvm.regs(:non_aligned_big_msb0).with_msb0.inspect(fancy_output: false) %>
